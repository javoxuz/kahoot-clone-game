// Color Palette - Onlayn Jamoaviy Test O'yini (Kahoot Clone)
export const COLORS = {
  primary: '#1368CE', // Moviy (Blue) - Main brand color
  secondary: '#D81B60', // Pushti (Pink) - Accent
  success: '#43A047', // Yashil (Green) - Correct answer
  error: '#E53935', // Qizil (Red) - Wrong answer
  warning: '#FFA000', // Oltin (Gold) - Achievements/Winner
  background: '#F5F5F5', // Och kulrang (Light gray) - Background
  text: '#212121', // Qora (Black) - Main text
  textSecondary: '#757575', // Kulrang (Gray) - Secondary text
  white: '#FFFFFF',
};

// Typography - Plus Jakarta Sans Font
export const TYPOGRAPHY = {
  heading1: {
    fontSize: '48px',
    fontWeight: 700,
    lineHeight: 1.2,
  },
  heading2: {
    fontSize: '36px',
    fontWeight: 600,
    lineHeight: 1.3,
  },
  heading3: {
    fontSize: '24px',
    fontWeight: 600,
    lineHeight: 1.4,
  },
  body: {
    fontSize: '16px',
    fontWeight: 400,
    lineHeight: 1.5,
  },
  bodyStrong: {
    fontSize: '16px',
    fontWeight: 700,
    lineHeight: 1.5,
  },
  nav: {
    fontSize: '18px',
    fontWeight: 600,
    lineHeight: 1.4,
  },
};

// Spacing
export const SPACING = {
  xs: '4px',
  sm: '8px',
  md: '16px',
  lg: '24px',
  xl: '32px',
  xxl: '48px',
  xxxl: '60px',
};

// Border Radius
export const BORDER_RADIUS = {
  sm: '8px',
  md: '12px',
  lg: '16px',
  full: '9999px',
};

// Shadows
export const SHADOWS = {
  sm: '0 1px 2px rgba(0, 0, 0, 0.05)',
  md: '0 4px 6px rgba(0, 0, 0, 0.1)',
  lg: '0 10px 15px rgba(0, 0, 0, 0.1)',
  xl: '0 20px 25px rgba(0, 0, 0, 0.1)',
};

// Transitions
export const TRANSITIONS = {
  fast: '150ms ease-in-out',
  default: '300ms ease-in-out',
  slow: '500ms ease-in-out',
};

// Game Button Colors (4 Answer Options)
export const GAME_BUTTON_COLORS = {
  option1: '#1368CE', // Blue
  option2: '#D81B60', // Pink
  option3: '#43A047', // Green
  option4: '#FFA000', // Gold
};
